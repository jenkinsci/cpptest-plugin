description.pattern=<a href="{0}">Fileset 'includes'</a> \
                 setting that specifies the generated raw Cpptest XML report files, such as '**/cpptest-result.xml'. \
                 Basedir of the fileset is <a href="ws/">the workspace root</a>. \
                 If no value is set, then the default '**/cpptest-result.xml' is used. Be sure not to include any \
             non-report files into this pattern.
